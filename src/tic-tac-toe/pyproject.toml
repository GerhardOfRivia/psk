[tool.poetry]
name = "psk-tic-tac-toe"
version = "0.1.0"
description = ""
authors = [""]
readme = "README.md"
packages = [{include = "tic_tac_toe"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
